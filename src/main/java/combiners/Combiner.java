package combiners;

public interface Combiner {
     /*
        postgre mongo - both to oracle - then merge

        return connection

        generation scripts to create db

        connection через интерфейс
        фабрика с сингелтонами

        liquid base - посмотреть

        распараллелить работу с табличками (thread pool executor) -- each thread on table
    */
}
